name: portfolio-prod

# ---------- Backend Defaults ----------
x-backend-defaults: &backend-defaults
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "3"
  volumes:
    - prod_media_data:/app/media
    - prod_static_data:/app/staticfiles
  networks:
    - portfolio_network
  environment: &backend-defaults-env
    DEBUG: "False"
    DOCKER_ENV: "true"
    PYTHONPATH: "."
    TZ: "Europe/Warsaw"
    SECRET_KEY: ${SECRET_KEY}
    DB_NAME: portfolio_prod
    DB_USER: ${DB_USER}
    DB_PASSWORD: ${DB_PASSWORD}
    DB_HOST: ${DB_HOST:-db}
    DB_PORT: 5432
    ADMIN_DOMAIN: ${ADMIN_DOMAIN}
    API_DOMAIN: ${API_DOMAIN}
    CSRF_COOKIE_DOMAIN: ${CSRF_COOKIE_DOMAIN}
    ALLOWED_HOSTS: ${ALLOWED_HOSTS}
    CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
    CSRF_TRUSTED_ORIGINS: ${CSRF_TRUSTED_ORIGINS}
    EMAIL_HOST: ${EMAIL_HOST}
    EMAIL_PORT: ${EMAIL_PORT}
    EMAIL_USE_TLS: ${EMAIL_USE_TLS}
    EMAIL_HOST_USER: ${EMAIL_HOST_USER}
    EMAIL_HOST_PASSWORD: ${EMAIL_HOST_PASSWORD}
    DEFAULT_FROM_EMAIL: ${DEFAULT_FROM_EMAIL}
    CONTACT_EMAIL: ${CONTACT_EMAIL}
    SESSION_COOKIE_DOMAIN: ${SESSION_COOKIE_DOMAIN}
    SENTRY_DSN: ${SENTRY_DSN}
    ENVIRONMENT: prod
    MONITORING_LLM_PROVIDER: ${MONITORING_LLM_PROVIDER:-gpt}
    PROJECT_OWNER: ${PROJECT_OWNER}
    REDIS_PASSWORD: ${REDIS_PASSWORD}
    SENTRY_TRACES_SAMPLE_RATE: ${SENTRY_TRACES_SAMPLE_RATE:-0.1}
    OPENAI_API_KEY: ${OPENAI_API_KEY}
    SKIP_PERMISSION_FIX: ${SKIP_PERMISSION_FIX:-false}
  depends_on:
    db:
      condition: service_healthy
    redis:
      condition: service_started

networks:
  portfolio_network:
  traefik_proxy:
    external: true

services:
  # ---------- Database ----------
  db:
    image: postgres:18
    restart: always
    networks:
      - portfolio_network
    environment:
      POSTGRES_DB: portfolio_prod
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER} -d portfolio_prod" ]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - postgres18_data:/var/lib/postgresql

  # ---------- Database Backup ----------
  db-backup:
    image: prodrigestivill/postgres-backup-local:18
    restart: always
    networks:
      - portfolio_network
    environment:
      POSTGRES_HOST: db
      POSTGRES_DB: portfolio_prod
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      SCHEDULE: "@daily"
      BACKUP_KEEP_DAYS: 7
      BACKUP_SUFFIX: ".sql.gz"
    volumes:
      - ${DB_BACKUP_DIR:-/var/backups/portfolio/prod}:/backups
    depends_on:
      db:
        condition: service_healthy

  # ---------- Redis ----------
  redis:
    build:
      context: ./docker/redis
    restart: always
    networks:
      - portfolio_network
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a $$REDIS_PASSWORD ping || redis-cli ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ---------- Celery Worker ----------
  celery-worker:
    <<: *backend-defaults
    image: ${ENVIRONMENT}-worker:${TAG}
    restart: always
    command: celery -A settings worker -l info -Q celery,monitoring --concurrency=${CELERY_WORKERS:-4}
    environment:
      <<: *backend-defaults-env
      MONITORING_LLM_PROVIDER: ${MONITORING_LLM_PROVIDER:-gpt}
    volumes:
      - prod_media_data:/app/media
      - prod_static_data:/app/staticfiles
      - ${DOCKER_LOGS_DIR:-./logs}:/app/docker-logs
    healthcheck:
      test: ["CMD-SHELL", "celery -A settings inspect ping -d celery@$$HOSTNAME"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ---------- Celery Beat ----------
  celery-beat:
    <<: *backend-defaults
    image: ${ENVIRONMENT}-be:${TAG}
    restart: always
    command: celery -A settings beat -l info -s /app/celerybeat/celerybeat-schedule
    environment:
      <<: *backend-defaults-env
    volumes:
      - prod_media_data:/app/media
      - prod_static_data:/app/staticfiles
      - celerybeat_data:/app/celerybeat

  # ---------- Frontend ----------
  fe:
    image: ${ENVIRONMENT}-fe:${TAG}
    restart: "always"
    networks:
      - portfolio_network
    expose:
      - "8080"
    environment:
      TZ: "Europe/Warsaw"
      SITE_DOMAIN: ${SITE_DOMAIN}
      SSR_CACHE_INVALIDATION_TOKEN: ${SSR_CACHE_INVALIDATION_TOKEN}
      PORT: 8080
      FRONTEND_PORT: 8080
      NODE_ENV: production
      API_URL: https://${API_DOMAIN}
      SSR_API_URL: http://be:8000
      PROJECT_OWNER: ${PROJECT_OWNER:-Lukasz}
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ---------- Backend ----------
  be:
    <<: *backend-defaults
    image: ${ENVIRONMENT}-be:${TAG}
    restart: always
    environment:
      <<: *backend-defaults-env
      SSR_CACHE_INVALIDATION_URL: ${SSR_CACHE_INVALIDATION_URL:-http://fe:8080/internal/cache/invalidate}
      SSR_CACHE_INVALIDATION_TOKEN: ${SSR_CACHE_INVALIDATION_TOKEN}
    healthcheck:
      test: ["CMD-SHELL", "curl -f -H 'X-Forwarded-Proto: https' http://127.0.0.1:8000/health || exit 1"]
      interval: 10s
      timeout: 5s


  # ---------- Release Job ----------
  release:
    <<: *backend-defaults
    image: ${ENVIRONMENT}-be:${TAG}
    restart: "no"
    environment:
      <<: *backend-defaults-env
      SSR_CACHE_INVALIDATION_URL: ${SSR_CACHE_INVALIDATION_URL:-http://fe:8080/internal/cache/invalidate}
      SSR_CACHE_INVALIDATION_TOKEN: ${SSR_CACHE_INVALIDATION_TOKEN}
    command: >
      sh -c "
        python manage.py migrate --noinput &&
        python manage.py seed_settings &&
        python manage.py collectstatic --noinput
      "

  # ---------- Nginx ----------
  # Service name must be "nginx" — deploy.sh uses: docker compose exec -T nginx nginx -s reload
  nginx:
    image: ${ENVIRONMENT}-nginx:${TAG}
    restart: always
    read_only: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /var/cache/nginx
      - /var/run
      - /tmp
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - portfolio_network
      - traefik_proxy
    labels:
      - "traefik.enable=true"
      # Main Site Router
      - "traefik.http.routers.portfolio-prod.rule=Host(`${SITE_DOMAIN}`)"
      - "traefik.http.routers.portfolio-prod.entrypoints=websecure"
      - "traefik.http.routers.portfolio-prod.tls=true"
      - "traefik.http.routers.portfolio-prod.tls.certresolver=letsencrypt"
      - "traefik.http.routers.portfolio-prod.service=portfolio-prod"

      # Admin Router
      - "traefik.http.routers.portfolio-prod-admin.rule=Host(`admin.${SITE_DOMAIN}`)"
      - "traefik.http.routers.portfolio-prod-admin.entrypoints=websecure"
      - "traefik.http.routers.portfolio-prod-admin.tls=true"
      - "traefik.http.routers.portfolio-prod-admin.tls.certresolver=letsencrypt"
      - "traefik.http.routers.portfolio-prod-admin.service=portfolio-prod"

      # Middlewares (Security Headers, HSTS, and Rate Limiting)
      - "traefik.http.routers.portfolio-prod.middlewares=public-security@file"
      - "traefik.http.routers.portfolio-prod-admin.middlewares=admin-api-security@file,rate-limit@file"

      # Explicit service port (Nginx listens on 80)
      - "traefik.http.services.portfolio-prod.loadbalancer.server.port=80"
    depends_on:
      - be
      - fe
    volumes:
      - ${NGINX_LOG_DIR:-/var/log/portfolio/nginx/prod}:/var/log/nginx
      - prod_media_data:/app/media:ro
      - prod_static_data:/app/staticfiles:ro
      - nginx_blocklist:/etc/nginx/blocklist:ro

volumes:
  postgres18_data:
    name: portfolio_prod_db_data
    external: true
  prod_media_data:
    name: portfolio_prod_media_data
    external: true
  prod_static_data:
    name: portfolio_prod_static_data
    external: true
  celerybeat_data:
    name: portfolio_prod_celerybeat_data
    external: true
  nginx_blocklist:
    name: portfolio_prod_nginx_blocklist
    external: true
