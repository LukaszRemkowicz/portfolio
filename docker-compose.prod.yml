networks:
  portfolio_network:


# ---------- Backend Defaults ----------
x-backend-defaults: &backend-defaults
  volumes:
    - prod_media_data:/app/media
    - prod_static_data:/app/staticfiles
  networks:
    - portfolio_network
  environment: &backend-defaults-env
    DEBUG: "False"
    DOCKER_ENV: "true"
    PYTHONPATH: "."
    SECRET_KEY: ${SECRET_KEY}
    DB_NAME: ${DB_NAME}
    DB_USER: ${DB_USER}
    DB_PASSWORD: ${DB_PASSWORD}
    DB_HOST: ${DB_HOST:-db}
    DB_PORT: 5432
    ADMIN_DOMAIN: ${ADMIN_DOMAIN}
    API_DOMAIN: ${API_DOMAIN}
    CSRF_COOKIE_DOMAIN: ${CSRF_COOKIE_DOMAIN}
    ALLOWED_HOSTS: ${ALLOWED_HOSTS}
    CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
    CSRF_TRUSTED_ORIGINS: ${CSRF_TRUSTED_ORIGINS}
    EMAIL_HOST: ${EMAIL_HOST}
    EMAIL_PORT: ${EMAIL_PORT}
    EMAIL_USE_TLS: ${EMAIL_USE_TLS}
    EMAIL_HOST_USER: ${EMAIL_HOST_USER}
    EMAIL_HOST_PASSWORD: ${EMAIL_HOST_PASSWORD}
    DEFAULT_FROM_EMAIL: ${DEFAULT_FROM_EMAIL}
    CONTACT_EMAIL: ${CONTACT_EMAIL}
    SESSION_COOKIE_DOMAIN: ${SESSION_COOKIE_DOMAIN}
    SENTRY_DSN: ${SENTRY_DSN}
    ENVIRONMENT: ${ENVIRONMENT:-production}
    MONITORING_LLM_PROVIDER: ${MONITORING_LLM_PROVIDER:-gpt}
    PROJECT_OWNER: ${PROJECT_OWNER}
    REDIS_PASSWORD: ${REDIS_PASSWORD}
    SENTRY_TRACES_SAMPLE_RATE: ${SENTRY_TRACES_SAMPLE_RATE:-0.1}
    OPENAI_API_KEY: ${OPENAI_API_KEY}
    SKIP_PERMISSION_FIX: ${SKIP_PERMISSION_FIX:-false}
  depends_on:
    db:
      condition: service_healthy
    redis:
      condition: service_started


services:
  # ---------- Database ----------
  db:
    image: postgres:18
    restart: always
    networks:
      - portfolio_network
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}" ]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres18_data:/var/lib/postgresql

  # ---------- Redis ----------
  redis:
    build:
      context: ./docker/redis
    restart: always
    networks:
      - portfolio_network
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a $$REDIS_PASSWORD ping || redis-cli ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ---------- Celery Worker ----------
  celery-worker:
    <<: *backend-defaults
    image: ${ENVIRONMENT:-production}-worker:${TAG}
    restart: always
    command: celery -A settings worker -l info -Q celery,monitoring --concurrency=4
    environment:
      <<: *backend-defaults-env
      MONITORING_LLM_PROVIDER: ${MONITORING_LLM_PROVIDER:-gpt}
    volumes:
      - prod_media_data:/app/media
      - prod_static_data:/app/staticfiles
      - ${DOCKER_LOGS_DIR:-./logs}:/app/docker-logs
    healthcheck:
      test: ["CMD-SHELL", "celery -A settings inspect ping -d celery@$$HOSTNAME"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ---------- Celery Beat ----------
  celery-beat:
    <<: *backend-defaults
    image: ${ENVIRONMENT:-production}-be:${TAG}
    restart: always
    command: celery -A settings beat -l info -s /app/celerybeat/celerybeat-schedule
    environment:
      <<: *backend-defaults-env
    volumes:
      - prod_media_data:/app/media
      - prod_static_data:/app/staticfiles
      - celerybeat_data:/app/celerybeat

  # ---------- Frontend ----------
  fe:
    image: ${ENVIRONMENT:-production}-fe:${TAG}
    restart: "always"
    networks:
      - portfolio_network
    environment:
      VITE_GA_TRACKING_ID: ${GA_TRACKING_ID}
      VITE_SENTRY_DSN_FE: ${SENTRY_DSN_FE}
      VITE_ENVIRONMENT: ${ENVIRONMENT:-production}
      VITE_PROJECT_OWNER: ${PROJECT_OWNER}

  # ---------- Backend ----------
  be:
    <<: *backend-defaults
    image: ${ENVIRONMENT:-production}-be:${TAG}
    restart: always

  # ---------- Release Job ----------
  release:
    <<: *backend-defaults
    image: ${ENVIRONMENT:-production}-be:${TAG}
    restart: "no"
    command: >
      sh -c "
        python manage.py migrate --noinput &&
        python manage.py seed_settings &&
        python manage.py collectstatic --noinput
      "

  # ---------- Nginx ----------
  # Service name must be "nginx" — deploy.sh uses: docker compose exec -T nginx nginx -s reload
  nginx:
    image: nginx:1.25
    restart: always
    networks:
      - portfolio_network
    depends_on:
      - be
      - fe
    environment:
      # Injected by Doppler at runtime — envsubst replaces variable placeholders
      SITE_DOMAIN: ${SITE_DOMAIN}
      SSL_CERT_NAME: ${SSL_CERT_NAME}
      SSL_KEY_NAME: ${SSL_KEY_NAME}
      FRONTEND_PORT: ${FRONTEND_PORT:-8080}
      NGINX_ENVSUBST_OUTPUT_DIR: /etc/nginx
    volumes:
      - ./nginx/TEMPLATE.conf:/etc/nginx/templates/nginx.conf.template:ro
      - ${NGINX_SSL_DIR:-./nginx/ssl/certs}:/etc/nginx/ssl:ro
      - ${NGINX_LOG_DIR:-./nginx/logs}:/var/log/nginx
      - prod_media_data:/app/media:ro
      - prod_static_data:/app/staticfiles:ro
    ports:
      - "80:80"
      - "443:443"


volumes:
  postgres_data:
  postgres18_data:
  prod_media_data:
  prod_static_data:
  celerybeat_data:
