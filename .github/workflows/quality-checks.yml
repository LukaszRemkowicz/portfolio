name: Quality Checks

on:
  pull_request:
    branches: [main, dev]
  push:
    branches-ignore: [main]

env:
  PYTHON_VERSION: "3.13"
  NODE_VERSION: "20"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # Versioning
  version-check:
    name: Version bump check (dev → main)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR HEAD
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Log App Version
        run: |
          if [[ -f VERSION ]]; then
            VER=$(tr -d ' \t\r\n' < VERSION)
            echo "✅ PR introduces VERSION: $VER"
            echo "🏷️  Release tag would be: v$VER"
          else
            echo "⚠️ VERSION file not found"
          fi

      - name: Validate VERSION file and bump
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
          HEAD_REF: ${{ github.head_ref }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail

          if [[ "$EVENT_NAME" != "pull_request" ]] || \
            [[ "$BASE_REF" != "main" ]] || \
            [[ "$HEAD_REF" != "dev" ]]; then
            echo "ℹ️ Version check skipped (not dev → main PR)."
            exit 0
          fi

          VERSION_FILE="VERSION"

          echo "🔎 Version check (dev → main)"
          echo "📌 Base SHA: $BASE_SHA"
          echo "📌 Head SHA: $HEAD_SHA"

          # PR/DEV must have VERSION
          if ! git cat-file -e "${HEAD_SHA}:${VERSION_FILE}" 2>/dev/null; then
            echo "❌ ERROR: ${VERSION_FILE} is missing in PR branch (dev)."
            exit 1
          fi

          NEW="$(git show "${HEAD_SHA}:${VERSION_FILE}" | tr -d ' \t\r\n')"
          if ! [[ "$NEW" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "❌ ERROR: VERSION must be MAJOR.MINOR.PATCH (e.g. 1.0.2). Got: '$NEW'"
            exit 1
          fi

          # If main/base doesn't have VERSION yet -> bootstrap pass
          if ! git cat-file -e "${BASE_SHA}:${VERSION_FILE}" 2>/dev/null; then
            echo "⚠️  Bootstrap: ${VERSION_FILE} does not exist on base branch (main)."
            echo "✅ PR introduces VERSION: $NEW"
            echo "🏷️  Release tag would be: v$NEW"
            exit 0
          fi

          OLD="$(git show "${BASE_SHA}:${VERSION_FILE}" | tr -d ' \t\r\n')"
          if ! [[ "$OLD" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "❌ ERROR: Base VERSION is invalid on main. Got: '$OLD'"
            exit 1
          fi

          echo "🔢 Base version: $OLD"
          echo "🔢 PR version:   $NEW"

          if [[ "$OLD" == "$NEW" ]]; then
            # TODO: Uncomment this check in the next release.
            # Temporarily disabled to allow initial bootstrap/fix convergence.
            echo "⚠️ VERSION was not changed ($NEW), but proceeding for this release."
            # echo "❌ ERROR: VERSION was not changed in this PR (still $NEW)."
            # exit 1
          fi

          IFS='.' read -r oldM oldm oldp <<< "$OLD"
          IFS='.' read -r newM newm newp <<< "$NEW"

          # Require strictly greater
          # TODO: Uncomment this check in the next release.
          # Temporarily disabled to allow initial bootstrap/fix convergence.
          # if (( newM < oldM )) || \
          #   (( newM == oldM && newm < oldm )) || \
          #   (( newM == oldM && newm == oldm && newp <= oldp )); then
          #   echo "❌ ERROR: VERSION must increase. Base=$OLD PR=$NEW"
          #   exit 1
          # fi

          echo "✅ VERSION bump is valid: $OLD → $NEW"
          echo "🏷️  Release tag would be: v$NEW"

  # Frontend: Lint, Type Check, and Test
  frontend:
    needs: [version-check]
    name: Frontend Quality & Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Run ESLint
        working-directory: ./frontend
        run: npm run lint

      - name: Run Prettier check
        working-directory: ./frontend
        run: npm run format:check || (npx prettier --list-different 'src/**/*.{ts,tsx,css}' && exit 1)

      - name: TypeScript type check
        working-directory: ./frontend
        run: npx tsc --noEmit

      - name: Run tests
        working-directory: ./frontend
        run: npm test -- --coverage --watchAll=false
        env:
          API_URL: http://localhost:8000

      - name: Upload coverage reports
        if: github.event_name == 'push'
        uses: codecov/codecov-action@v4
        with:
          files: ./frontend/coverage/lcov.info
          flags: frontend
          token: ${{ secrets.CODECOV_TOKEN }}

  # Backend: Lint, Type Check, and Test
  backend:
    needs: [version-check]
    name: Backend Quality & Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    env:
      DB_HOST: localhost
      DB_NAME: test_db
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_PORT: 5432
      DEFAULT_FROM_EMAIL: test@example.com
      CONTACT_EMAIL: test@example.com
      SESSION_COOKIE_DOMAIN: .example.com

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: latest
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Cache Poetry virtualenv
        uses: actions/cache@v4
        with:
          path: backend/.venv
          key: ${{ runner.os }}-poetry-${{ hashFiles('backend/poetry.lock') }}
          restore-keys: |
            ${{ runner.os }}-poetry-

      - name: Install dependencies
        working-directory: ./backend
        run: poetry install --no-root --no-interaction

      - name: Run Python linting
        working-directory: ./backend
        run: |
          poetry run flake8 .
          poetry run black --check .

      - name: Run Python type checking
        working-directory: ./backend
        run: poetry run mypy .

      - name: Run tests
        working-directory: ./backend
        run: |
          poetry run pytest --cov=. --cov-report=xml

      - name: Upload coverage reports
        if: github.event_name == 'push'
        uses: codecov/codecov-action@v4
        with:
          files: ./backend/coverage.xml
          flags: backend
          token: ${{ secrets.CODECOV_TOKEN }}

  # Docker Build/Test
  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [frontend, backend]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Cache Docker Images
        uses: docker/bake-action@v6.0.0
        with:
          push: false
          load: true
          set: |
            *.cache-from=type=gha
            *.cache-to=type=gha,mode=max

  # Security Scanning
  security-scan:
    name: Security Analysis
    runs-on: ubuntu-latest
    needs: [frontend, backend]
    permissions:
      security-events: write
      actions: read
      contents: read
    env:
      DB_HOST: localhost
      DB_NAME: test_db
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_PORT: 5432
      DEFAULT_FROM_EMAIL: test@example.com
      CONTACT_EMAIL: test@example.com
      SESSION_COOKIE_DOMAIN: .example.com

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          scan-ref: "."
          format: "sarif"
          output: "trivy-results.sarif"

      - name: Upload Trivy results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: "trivy-results.sarif"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Run npm audit
        working-directory: ./frontend
        run: npm audit --audit-level=moderate

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: latest
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Cache Poetry virtualenv
        uses: actions/cache@v4
        with:
          path: backend/.venv
          key: ${{ runner.os }}-poetry-${{ hashFiles('backend/poetry.lock') }}
          restore-keys: |
            ${{ runner.os }}-poetry-

      - name: Install dependencies
        working-directory: ./backend
        run: poetry install --no-root --no-interaction

      - name: Backend safety check
        working-directory: ./backend
        run: |
          poetry run pip install safety
          poetry run safety check

  # PR Status Reporting
  pr-status:
    name: PR Status Report
    runs-on: ubuntu-latest
    needs: [frontend, backend, docker-build, security-scan]
    if: always() && github.event_name == 'pull_request'
    permissions:
      pull-requests: write

    steps:
      - name: Report Status
        uses: actions/github-script@v7
        with:
          script: |
            const results = {
              'Frontend (Lint/Types/Tests)': '${{ needs.frontend.result }}',
              'Backend (Lint/Types/Tests)': '${{ needs.backend.result }}',
              'Docker Build': '${{ needs.docker-build.result }}',
              'Security Analysis': '${{ needs.security-scan.result }}'
            };

            const emoji = (s) => s === 'success' ? '✅' : (s === 'failure' ? '❌' : '⏹️');

            let body = `## 🚀 CI/CD Pipeline Results\n\n| Check | Status |\n|---|---|\n`;
            for (const [name, res] of Object.entries(results)) {
              body += `| ${name} | ${emoji(res)} ${res} |\n`;
            }

            const allSuccess = Object.values(results).every(s => s === 'success');
            body += `\n${allSuccess ? '🎉 **All checks passed!**' : '⚠️ **Some checks failed. Please review the logs.**'}`;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(c => c.body.includes('CI/CD Pipeline Results'));

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body
              });
            }
