name: portfolio-stage

# ---------- Backend Defaults ----------
x-backend-defaults: &backend-defaults
  volumes:
    - portfolio_staging_media_data:/app/media
    - portfolio_staging_static_data:/app/staticfiles
  networks:
    - stage_network
    - traefik_proxy
  environment: &backend-defaults-env
    DEBUG: "False"
    DOCKER_ENV: "true"
    PYTHONPATH: "."
    SECRET_KEY: ${SECRET_KEY}
    DB_NAME: portfolio_stage
    DB_USER: ${DB_USER:-portfolio}
    DB_PASSWORD: ${DB_PASSWORD}
    DB_HOST: db
    DB_PORT: 5432
    ADMIN_DOMAIN: ${ADMIN_DOMAIN}
    API_DOMAIN: ${API_DOMAIN}
    SITE_DOMAIN: ${SITE_DOMAIN}
    CSRF_COOKIE_DOMAIN: ${CSRF_COOKIE_DOMAIN}
    ALLOWED_HOSTS: ${ALLOWED_HOSTS}
    CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
    CSRF_TRUSTED_ORIGINS: ${CSRF_TRUSTED_ORIGINS}
    EMAIL_HOST: ${EMAIL_HOST}
    EMAIL_PORT: ${EMAIL_PORT}
    EMAIL_USE_TLS: ${EMAIL_USE_TLS}
    EMAIL_HOST_USER: ${EMAIL_HOST_USER}
    EMAIL_HOST_PASSWORD: ${EMAIL_HOST_PASSWORD}
    DEFAULT_FROM_EMAIL: ${DEFAULT_FROM_EMAIL}
    CONTACT_EMAIL: ${CONTACT_EMAIL}
    SESSION_COOKIE_DOMAIN: ${SESSION_COOKIE_DOMAIN}
    SENTRY_DSN: ${SENTRY_DSN:-}
    ENABLE_SENTRY: ${ENABLE_SENTRY:-false}
    ENVIRONMENT: stage
    SENTRY_TRACES_SAMPLE_RATE: ${SENTRY_TRACES_SAMPLE_RATE:-0.0}
    OPENAI_API_KEY: ${OPENAI_API_KEY}
    PROJECT_OWNER: ${PROJECT_OWNER:-Lukasz}
    REDIS_URL: redis://redis:6379/1
    CELERY_BROKER_URL: redis://redis:6379/0
    CELERY_RESULT_BACKEND: redis://redis:6379/0
    SKIP_PERMISSION_FIX: ${SKIP_PERMISSION_FIX:-false}
  depends_on:
    db:
      condition: service_healthy
    redis:
      condition: service_started

networks:
  stage_network:
  traefik_proxy:
    external: true

services:
  # ---------- Database ----------
  db:
    image: postgres:18
    restart: always
    networks:
      - stage_network
    environment:
      POSTGRES_DB: portfolio_stage
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - portfolio_staging_db_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d portfolio_stage"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ---------- Redis ----------
  redis:
    build:
      context: ./docker/redis
    restart: always
    networks:
      - stage_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ---------- Celery Worker ----------
  celery-worker:
    <<: *backend-defaults
    image: stage-worker:${TAG}
    restart: always
    command: celery -A settings worker -l info -Q celery,monitoring --concurrency=${CELERY_WORKERS:-2}
    environment:
      <<: *backend-defaults-env
      MONITORING_LLM_PROVIDER: ${MONITORING_LLM_PROVIDER:-gpt}
    volumes:
      - portfolio_staging_media_data:/app/media
      - portfolio_staging_static_data:/app/staticfiles
      - ${DOCKER_LOGS_DIR:-/tmp/stage-docker-logs}:/app/docker-logs
    healthcheck:
      test: ["CMD-SHELL", "celery -A settings inspect ping -d celery@$$HOSTNAME"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ---------- Celery Beat ----------
  celery-beat:
    <<: *backend-defaults
    image: stage-be:${TAG}
    restart: always
    command: celery -A settings beat -l info -s /app/celerybeat/celerybeat-schedule
    environment:
      <<: *backend-defaults-env
    volumes:
      - portfolio_staging_media_data:/app/media
      - portfolio_staging_static_data:/app/staticfiles
      - portfolio_staging_celerybeat_data:/app/celerybeat

  # ---------- Frontend ----------
  fe:
    image: stage-fe:${TAG}
    restart: always
    networks:
      - stage_network
    volumes:
      - portfolio_staging_frontend_dist:/frontend_dist

  # ---------- Backend ----------
  be:
    <<: *backend-defaults
    image: stage-be:${TAG}
    restart: always
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "python3 -c \"import sys, urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=5); sys.exit(0)\" >/dev/null 2>&1 || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 3
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  # ---------- Release Job ----------
  release:
    <<: *backend-defaults
    image: stage-be:${TAG}
    restart: "no"
    command: >
      sh -c "
        python manage.py migrate --noinput &&
        python manage.py seed_settings &&
        python manage.py collectstatic --noinput
      "

  # ---------- Nginx ----------
  nginx:
    image: stage-nginx:${TAG}
    restart: always
    read_only: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /var/cache/nginx
      - /var/run
      - /tmp
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - stage_network
      - traefik_proxy
    volumes:
      - ${NGINX_LOG_DIR:-/var/log/portfolio/nginx/stage}:/var/log/nginx
      - portfolio_staging_media_data:/app/media:ro
      - portfolio_staging_static_data:/app/staticfiles:ro
      - portfolio_staging_nginx_blocklist:/etc/nginx/blocklist:ro
      - portfolio_staging_frontend_dist:/usr/share/nginx/html:ro
    labels:
      - "traefik.enable=true"
      # Main site routing
      - "traefik.http.routers.staging-site.rule=Host(`${SITE_DOMAIN}`)"
      - "traefik.http.routers.staging-site.entrypoints=websecure"
      - "traefik.http.routers.staging-site.tls=true"
      - "traefik.http.routers.staging-site.tls.certresolver=letsencrypt"
      - "traefik.http.routers.staging-site.middlewares=staging-whitelist@docker,staging-security@file"

      # Admin/API routing (Everything goes to Nginx now)
      - "traefik.http.routers.staging-api.rule=Host(`api.${SITE_DOMAIN}`) || Host(`admin.${SITE_DOMAIN}`)"
      - "traefik.http.routers.staging-api.entrypoints=websecure"
      - "traefik.http.routers.staging-api.tls=true"
      - "traefik.http.routers.staging-api.tls.certresolver=letsencrypt"
      - "traefik.http.routers.staging-api.middlewares=staging-whitelist@docker,staging-security@file,rate-limit@file"
      - "traefik.http.routers.staging-api.priority=2"

      - "traefik.http.services.staging-site.loadbalancer.server.port=80"

volumes:
  portfolio_staging_db_data:
    external: true
  portfolio_staging_media_data:
    external: true
  portfolio_staging_static_data:
    external: true
  portfolio_staging_celerybeat_data:
    external: true
  portfolio_staging_nginx_blocklist:
    external: true
  portfolio_staging_frontend_dist:
    external: true
