# Application
SECRET_KEY=change-me-in-production
DEBUG=True

# Database
DB_ENGINE=django.db.backends.postgresql
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# Domain Configuration
ADMIN_DOMAIN=admin.portfolio.local
API_DOMAIN=portfolio.local
CSRF_COOKIE_DOMAIN=.portfolio.local
ALLOWED_HOSTS=admin.portfolio.local,portfolio.local,localhost,127.0.0.1.  # dont use on production
CORS_ALLOWED_ORIGINS=https://portfolio.local,http://portfolio.local,http://portfolio.local:3000 # dont use on production
CSRF_TRUSTED_ORIGINS=https://portfolio.local,http://portfolio.local  # dont use on production

# Email Configuration (Required)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=
CONTACT_EMAIL=

# AI
OPENAI_API_KEY=

# Sentry
SENTRY_DSN=
SENTRY_DSN_FE=
